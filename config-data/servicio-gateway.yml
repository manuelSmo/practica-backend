server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: servicio-cliente
          uri: http://localhost:8091
          predicates:
            - Path=/clientes/**
        - id: servicio-foto
          uri: http://localhost:8092
          predicates:
            - Path=/fotos/**