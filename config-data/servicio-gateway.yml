server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: servicio-ccliente
          uri: lb://servicio-cliente
          predicates:
            - Path=/clientes/**
        - id: servicio-foto
          uri: lb://servicio-foto
          predicates:
            - Path=/fotos/**